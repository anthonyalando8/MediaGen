{
  "name": "seabytes-editor",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "lint:boundaries": "depcruise --config dep-cruiser.cjs packages apps",
    "docker:up": "docker compose up --build",
    "docker:up:prod": "docker compose -f docker-compose.yml up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "dependency-cruiser": "^16.4.0"
  }
}
