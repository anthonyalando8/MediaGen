// effects — Phase 2, not yet implemented.
// Effect + transition shader registry (blur, grade, slam, wipe...).
// See docs/architecture and the Phase 1 blueprint for this package's contract.
export * from "./registry"
export * from "./register-builtins"
